{
  "name": "agent-run-lens",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first black box recorder for artificial intelligence agent runs.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "build": "corepack pnpm -r --if-present build",
    "test": "vitest run",
    "check": "corepack pnpm -r --if-present check",
    "verify": "corepack pnpm build && corepack pnpm test",
    "agent-run-lens": "corepack pnpm --filter @agent-run-lens/cli agent-run-lens",
    "traceforge": "corepack pnpm --filter @agent-run-lens/cli traceforge"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
